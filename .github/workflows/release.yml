name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Verify, publish, and release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: gradle/actions/wrapper-validation@v6.2.0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v6.2.0
      - name: Verify tag matches mod version
        shell: bash
        run: |
          mod_version="$(sed -n 's/^mod_version=//p' gradle.properties)"
          if [[ ! "${mod_version}" =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "mod_version must be a stable SemVer x.y.z value: ${mod_version}" >&2
            exit 1
          fi
          if [[ "${GITHUB_REF_NAME}" != "v${mod_version}" ]]; then
            echo "Tag ${GITHUB_REF_NAME} does not match mod_version=${mod_version}." >&2
            exit 1
          fi
      - name: Check and build
        run: ./gradlew --no-daemon clean check build
      - name: Runtime Mixin and command smoke
        shell: bash
        run: |
          mkdir -p run/server
          printf 'eula=true\n' > run/server/eula.txt
          timeout 240s bash -o pipefail -c \
            'printf "atmons_spawn_controller policy easy_vote\natmons_spawn_controller status\natmons_spawn_controller policy stock\natmons_spawn_controller policy peaceful\natmons_spawn_controller status\nstop\n" | ./gradlew --no-daemon runServer'
          grep -F 'Runtime-loaded required Mixin target net.minecraft.world.level.NaturalSpawner' \
            run/server/logs/latest.log
          grep -F 'All the Mons spawn policy is now easy_vote' run/server/logs/latest.log
          grep -F 'All the Mons spawn policy is now stock' run/server/logs/latest.log
          grep -F 'policy=peaceful' run/server/logs/latest.log
          grep -F 'Stopping the server' run/server/logs/latest.log
      - name: Publish Maven package
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./gradlew --no-daemon publish
      - name: Create production JAR checksum
        shell: bash
        run: |
          mod_version="$(sed -n 's/^mod_version=//p' gradle.properties)"
          production_jar="build/libs/allthemons-spawn-controller-${mod_version}.jar"
          test -f "${production_jar}"
          (
            cd build/libs
            sha256sum "allthemons-spawn-controller-${mod_version}.jar"
          ) > SHA256SUMS.txt
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          mod_version="$(sed -n 's/^mod_version=//p' gradle.properties)"
          production_jar="build/libs/allthemons-spawn-controller-${mod_version}.jar"
          gh release create "${GITHUB_REF_NAME}" \
            "${production_jar}" \
            SHA256SUMS.txt \
            --generate-notes \
            --verify-tag \
            --title "All the Mons Spawn Controller ${GITHUB_REF_NAME}"
